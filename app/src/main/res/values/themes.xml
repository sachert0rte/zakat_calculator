<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base app theme -->
    <style name="Base.Theme.ZakatCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Use custom toolbar style -->
        <item name="toolbarStyle">@style/MyToolbarStyle</item>
    </style>

    <!-- Main App Theme -->
    <style name="Theme.ZakatCalculator" parent="Base.Theme.ZakatCalculator" />

    <!-- Toolbar title style -->
    <style name="MyToolbarStyle" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@android:color/white</item>
    </style>

    <!-- Make overflow icon (three dots) white -->
    <style name="ToolbarIconWhite" parent="">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:colorControlNormal">@android:color/white</item>
    </style>

</resources>
